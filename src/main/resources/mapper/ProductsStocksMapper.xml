<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youke.dao.ProductsStocksMapper">
  <resultMap id="BaseResultMap" type="com.youke.entity.ProductsStocks">
    <!--@mbg.generated-->
    <!--@Table products_stocks-->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="products_id"  property="productsId" />
    <result column="picture"  property="picture" />
    <result column="price"  property="price" />
    <result column="stocks"  property="stocks" />
  </resultMap>
  <sql id="Base_Column_List">
    <!--@mbg.generated-->
    id, products_id, picture, price, stocks
  </sql>

  <select id="getProductsStocks" resultType="com.youke.entity.ProductsStocks">
    SELECT
       <include refid="Base_Column_List" />
    FROM products_options_rel_stocks AS a
    LEFT JOIN products_stocks AS b ON a.stock_id=b.id
    WHERE
        a.option_id in
        <foreach collection="list" item="item" index="index" open="(" close=")" separator=",">
          #{item.optionId,jdbcType=INTEGER}
        </foreach>
  </select>
</mapper>